<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dama.damaService.domain.mapper.UserEntityMapper">

    <select id="find" resultType="UserEntity">
        SELECT
        id,
        name,
        password
        FROM
        user_table
    </select>

</mapper>
